spring.datasource.url=${DATABASE_URL:jdbc:postgresql://127.0.0.1:5432/post-service-training-test}
spring.datasource.username=${POSTGRES_USER_NAME:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:password}
spring.datasource.driver-class-name=org.postgresql.Driver